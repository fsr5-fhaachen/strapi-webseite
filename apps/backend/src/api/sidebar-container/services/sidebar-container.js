'use strict';

/**
 * sidebar-container service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sidebar-container.sidebar-container');
